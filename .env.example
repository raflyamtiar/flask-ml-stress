# Copy this file to `.env` (ignored by git) and update values for local development.
# Example environment variables used by the app.

# SECRET_KEY is used by Flask for session signing — set to a strong random value in production.
SECRET_KEY=replace-me-with-a-secret
FLASK_ENV=development
FRONTEND_URL=http://localhost:3000
